[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
addopts =
    --reuse-db
    --strict-markers
    --tb=short
    -ra
markers =
    slow: marks tests as slow
    integration: hits external services (mocked in CI)
testpaths = apps tests